{
  "name": "@coordinator/package-queue-orchestrator",
  "version": "0.1.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "clean": "rm -rf dist tsconfig.tsbuildinfo",
    "test": "vitest run",
    "test:watch": "vitest",
    "worker": "node --loader ts-node/esm src/worker.ts"
  },
  "dependencies": {
    "@temporalio/workflow": "^1.10.0",
    "@temporalio/activity": "^1.10.0",
    "@temporalio/worker": "^1.10.0",
    "@temporalio/client": "^1.10.0",
    "dotenv": "^17.2.3"
  },
  "devDependencies": {
    "@temporalio/testing": "^1.10.0",
    "@temporalio/nyc-test-coverage": "^1.10.0",
    "@types/node": "^22.7.5",
    "typescript": "^5.6.3",
    "vitest": "^1.6.0"
  }
}
