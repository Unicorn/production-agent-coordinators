{
  "__NOTE__": "Copy this file to build-env.json and fill in real values",
  "__WARNING__": "build-env.json is gitignored - it will NOT be committed",
  "__SECRETS__": "NEVER store secrets in this file. Secrets are loaded from environment variables (NPM_TOKEN). System environment variables for production. .env file fallback for dev/staging only. Hard fail at startup if required env vars are missing.",

  "npmRegistry": "https://registry.npmjs.org/",

  "workspaceRoot": "/Users/mattbernier/projects/tools",
  "maxConcurrentBuilds": 4,

  "temporal": {
    "address": "localhost:7233",
    "namespace": "default",
    "taskQueue": "agent-coordinator-queue"
  },

  "testing": {
    "enableCoverage": true,
    "minCoveragePercent": 80,
    "failOnError": true
  },

  "publishing": {
    "dryRun": false,
    "requireTests": true,
    "requireCleanWorkingDirectory": true
  }
}
